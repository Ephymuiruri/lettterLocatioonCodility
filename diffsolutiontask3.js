function digitadd(arr){
    for(no of arr){
       const noArray = no.toString().split('')
       const sumArray =[]
       console.log(noArray)
       for(let i=0;i < noArray.length;i++){
           sumArray.push(parseInt(noArray[i]))
       }
    }
   }
   digitadd([1, 10, 12, 3])